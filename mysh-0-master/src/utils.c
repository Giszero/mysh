#include "utils.h"
#include <stdio.h>
#include <string.h>

void mysh_parse_command(const char* command,
                        int *argc, char*** argv)
{
  
int num = 0;
int num1 = 0;
int i,j = 0;

char ch;

printf("input");
gets(command);

for(i=0;i<strlen(str);i++)
{
ch = command[i];
if(ch == ' ')
num++
}
num + 1 = num1;
  
for(j=0;str(j) != '\0' ;j++)
{
ch = command[i];
if(command[j] == ' ') puts(" ")
else putchar command[j]
}
  


printf (argc == %d \n, num1);
  
}
